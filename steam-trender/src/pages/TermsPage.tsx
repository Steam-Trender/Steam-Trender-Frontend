import React from "react";
import { Markdown } from "../components/Markdown";

const TermsPage = () => {
    return (
        <>
            <Markdown file={"terms"} />
        </>
    );
};

export default TermsPage;
