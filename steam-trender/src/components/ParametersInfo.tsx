import React from "react";

export const ParametersInfo = () => {
    return (
        <p>
            Here is a brief description of the params for this page & their
            (default values), required are marked with
            <span className="text-primary fw-bold">*</span>.
        </p>
    );
};
